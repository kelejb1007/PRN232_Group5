@model Intelligence_Book_WEB.Models.CategoryViewModel
@{
    ViewData["Title"] = "Create Category";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<link rel="stylesheet" href="~/css/CategoryIndex.css" asp-append-version="true" />
<link rel="stylesheet" href="~/css/CategoryCreate.css" asp-append-version="true" />

<div class="category-page">
    <div class="edit-category-header">
        <h1>Create Category</h1>
    </div>

    <div class="create-form-container">
        <div class="form-card">

            <form asp-controller="CategoryPage" asp-action="Create" method="post">
                @Html.AntiForgeryToken()

                <div class="form-group">
                    <label asp-for="CategoryName" class="form-label">Category Name</label>
                    <input asp-for="CategoryName"
                           class="form-input"
                           placeholder="Enter category name" />
                    <span asp-validation-for="CategoryName" class="validation-msg"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Description" class="form-label">Description</label>
                    <textarea asp-for="Description"
                              class="form-input form-textarea"
                              placeholder="Enter description"
                              rows="4"></textarea>
                    <span asp-validation-for="Description" class="validation-msg"></span>
                </div>

                <div class="form-actions">
                    <a asp-controller="CategoryPage" asp-action="Index" class="btn-cancel">
                        Cancel
                    </a>
                    <button type="submit" class="btn-save">
                        Save Category
                    </button>
                </div>
            </form>

        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
